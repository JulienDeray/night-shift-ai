---
phase: 10-daemon-wiring-and-legacy-cleanup
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tests/integration/inbox.test.ts
  - tests/integration/status.test.ts
  - tests/integration/schedule.test.ts
autonomous: true
requirements: [WIRE-01, WIRE-02]
gap_closure: true

must_haves:
  truths:
    - "All 15 previously-failing integration tests pass with exit code 0"
    - "nightshift.yaml configs in test fixtures use agents:/schedule: schema (no recurring: key)"
    - "schedule.test.ts assertions match the current CLI output text"
  artifacts:
    - path: "tests/integration/inbox.test.ts"
      provides: "Valid nightshift.yaml fixture using agents/schedule schema"
      contains: "agents_dir"
    - path: "tests/integration/status.test.ts"
      provides: "Valid nightshift.yaml fixture using agents/schedule schema"
      contains: "agents_dir"
    - path: "tests/integration/schedule.test.ts"
      provides: "Schedule entries with agent/cron/enabled fields; assertions matching current CLI output"
      contains: "schedule:"
  key_links:
    - from: "tests/integration/inbox.test.ts"
      to: "src/core/config.ts"
      via: "nightshift.yaml parsed by loadConfig()"
      pattern: "agents_dir.*agents.*schedule"
    - from: "tests/integration/schedule.test.ts"
      to: "src/cli/commands/schedule.ts"
      via: "CLI output text assertions"
      pattern: "No schedule entries configured"
---

<objective>
Fix 3 integration test files that still write nightshift.yaml configs with the legacy `recurring:` key, which the updated strict Zod schema rejects with "Unrecognized key". This causes 15 test failures across inbox, status, and schedule integration tests.

Purpose: Close the single verification gap from Phase 10 — "All existing integration tests pass on the new dispatch path"
Output: 3 updated test files, 0 test failures
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-daemon-wiring-and-legacy-cleanup/10-02-SUMMARY.md
@.planning/phases/10-daemon-wiring-and-legacy-cleanup/10-VERIFICATION.md

<interfaces>
<!-- Current valid config schema from src/core/config.ts (ConfigSchema is .strict()) -->
<!-- These are the ONLY top-level keys the schema accepts: -->
<!--   workspace, inbox, max_concurrent, default_timeout, beads, daemon, -->
<!--   agents_dir, agents, schedule, one_off_defaults, ntfy -->
<!-- The key "recurring" is REJECTED — it does not exist in the schema -->

<!-- Minimal valid config for tests that don't need agents/schedule: -->
```yaml
workspace: ./workspace
max_concurrent: 2
beads:
  enabled: false
daemon:
  poll_interval_ms: 30000
  heartbeat_interval_ms: 10000
  log_retention_days: 30
agents_dir: ./agents
agents: []
schedule: []
one_off_defaults:
  timeout: "30m"
  max_budget_usd: 5.00
```

<!-- Schedule entry shape (ScheduleEntrySchema is .strict()): -->
<!-- agent: string, cron: string, variables?: Record, enabled: boolean (default true), notify?: boolean -->

<!-- AgentDeclaration shape (AgentDeclarationSchema is .strict()): -->
<!-- name: string (kebab-case), notify?: boolean, variables?: Record, fallback_categories?: string[] -->

<!-- CLI schedule command empty-state output: -->
<!-- "No schedule entries configured." -->
<!-- (was "No recurring tasks" in v1.0) -->

<!-- Schedule table columns: Name, Schedule, Enabled, Notify, Next Run -->
<!-- Each row: [entry.agent, entry.cron, enabled?"yes":"no", notify?"yes":"-", nextRunStr] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update integration test config fixtures to use new schema</name>
  <files>
    tests/integration/inbox.test.ts
    tests/integration/status.test.ts
    tests/integration/schedule.test.ts
  </files>
  <action>
Update `writeConfig()` in `inbox.test.ts` and `status.test.ts` to replace the `recurring: []` line with:
```yaml
agents_dir: ./agents
agents: []
schedule: []
```
Keep all other config keys unchanged.

Rewrite `schedule.test.ts` more substantially:
1. First test ("shows 'No recurring tasks'..."): Change assertion from `"No recurring tasks"` to `"No schedule entries configured"`.
2. Second test ("displays recurring tasks in a table..."): Replace the config block that uses `recurring:` with old fields (`name:`, `schedule:`, `prompt:`, `timeout:`, `max_budget_usd:`) with the new schema:
   ```yaml
   agents_dir: ./agents
   agents:
     - name: daily-standup
   schedule:
     - agent: daily-standup
       cron: "30 9 * * 1-5"
       enabled: true
   ```
   Update assertions: keep `"daily-standup"` and `"30 9 * * 1-5"` checks. Remove the assertions for `"15m"` and `"$2.00"` (these fields don't exist on schedule entries — the schedule command shows agent, cron, enabled, notify, next run). Add an assertion for `"yes"` (the enabled column).
3. Third test ("uses default timeout..."): This test concept is obsolete — schedule entries don't have timeout or budget fields. Replace with a test for a disabled schedule entry: write a config with one enabled and one disabled schedule entry, assert the output contains `"disabled"` for the disabled entry and does NOT contain `"disabled"` for the enabled one (the enabled entry shows a real next-run time).
   ```yaml
   agents_dir: ./agents
   agents:
     - name: active-agent
     - name: paused-agent
   schedule:
     - agent: active-agent
       cron: "0 3 * * *"
       enabled: true
     - agent: paused-agent
       cron: "0 4 * * *"
       enabled: false
   ```
   Assert `"active-agent"` is in output, `"paused-agent"` is in output, and `"disabled"` is in output. Check exit code 0.

Do NOT change anything about the `makeReport()` helper in inbox.test.ts — it generates markdown report files that are read from disk, not parsed by config. The report format is independent of the config schema.
  </action>
  <verify>
    <automated>cd /Users/julienderay/code/night-shift && npx vitest run tests/integration/inbox.test.ts tests/integration/status.test.ts tests/integration/schedule.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 integration tests across the 3 files pass (6 inbox + 3 status + 3 schedule). Zero "Unrecognized key" errors. The full test suite (npx vitest run) shows 0 failures.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/integration/inbox.test.ts tests/integration/status.test.ts tests/integration/schedule.test.ts` — all tests pass
2. `npx vitest run` — full suite passes (384/384, 0 failures)
3. `grep -r "recurring" tests/integration/` — returns zero matches (no legacy config keys remain)
</verification>

<success_criteria>
- 15 previously-failing integration tests now pass
- No test file references the `recurring:` config key
- Full test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/10-daemon-wiring-and-legacy-cleanup/10-03-SUMMARY.md`
</output>
