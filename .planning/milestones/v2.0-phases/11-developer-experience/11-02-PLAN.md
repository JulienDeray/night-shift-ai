---
phase: 11-developer-experience
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tests/unit/scaffold.test.ts
  - tests/integration/agent-commands.test.ts
autonomous: true
requirements:
  - DX-01
  - DX-02
  - DX-03

must_haves:
  truths:
    - "scaffoldAgent creates directory, manifest.yaml, and prompt files"
    - "scaffoldAgent throws on duplicate name without --force"
    - "scaffoldAgent overwrites with --force"
    - "scaffoldAgent updates nightshift.yaml when it exists"
    - "scaffoldAgent warns when nightshift.yaml is missing"
    - "agent validate exits 0 for a valid agent"
    - "agent validate exits 1 for an invalid manifest"
    - "agent validate warns (not errors) on missing env vars"
    - "agent list shows table with configured and unregistered agents"
    - "agent list --json outputs JSON array"
  artifacts:
    - path: "tests/unit/scaffold.test.ts"
      provides: "Unit tests for scaffoldAgent logic"
      min_lines: 80
    - path: "tests/integration/agent-commands.test.ts"
      provides: "Integration tests for all 4 agent CLI commands"
      min_lines: 100
  key_links:
    - from: "tests/unit/scaffold.test.ts"
      to: "src/agent/scaffold.ts"
      via: "import scaffoldAgent"
      pattern: "scaffoldAgent"
    - from: "tests/integration/agent-commands.test.ts"
      to: "src/cli/commands/agent.ts"
      via: "CLI subprocess invocation"
      pattern: "nightshift agent"
---

<objective>
Write unit and integration tests covering all four agent CLI commands and the scaffold logic.

Purpose: Verify DX-01 (init), DX-02 (list), DX-03 (validate) requirements with automated tests. Catch regressions in scaffold output and CLI behavior.
Output: `tests/unit/scaffold.test.ts`, `tests/integration/agent-commands.test.ts`
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-developer-experience/11-CONTEXT.md
@.planning/phases/11-developer-experience/11-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that tests validate against -->

From src/agent/scaffold.ts (created in Plan 01):
```typescript
export interface ScaffoldResult {
  agentDir: string;
  configUpdated: boolean;
  message: string;
}
export async function scaffoldAgent(name: string, options: { force?: boolean; base?: string }): Promise<ScaffoldResult>;
```

From src/agent/manifest-schema.ts:
```typescript
export const ManifestSchema: z.ZodObject<...>;
export type Manifest = z.infer<typeof ManifestSchema>;
```

From src/agent/manifest-loader.ts:
```typescript
export async function loadManifest(agentDir: string, agentsRoot: string): Promise<LoadedManifest>;
```

From src/agent/agent-types.ts:
```typescript
export function validateAgentName(name: string): { valid: boolean; error?: string };
```

From src/cli/formatters.ts:
```typescript
export function table(headers: string[], rows: string[][]): string;
```

From tests/integration/init-and-config.test.ts (pattern to follow for integration tests):
- Uses tmpdir for isolated test environment
- Spawns CLI via execFile (no shell) for safe subprocess invocation
- Asserts on stdout, stderr, exit codes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for scaffold logic</name>
  <files>tests/unit/scaffold.test.ts</files>
  <behavior>
    - Test 1: scaffoldAgent("my-agent", {base: tmpdir}) creates agents/my-agent/ directory
    - Test 2: scaffoldAgent creates manifest.yaml that passes ManifestSchema.safeParse()
    - Test 3: scaffoldAgent creates prompts/preamble.md, prompts/clone-stub.md, prompts/analyze.md
    - Test 4: analyze.md prompt contains JSON output instruction with result and summary fields
    - Test 5: scaffoldAgent throws on invalid name (e.g., "UPPERCASE" or "has spaces")
    - Test 6: scaffoldAgent throws when directory exists without --force
    - Test 7: scaffoldAgent succeeds with --force when directory exists (overwrites)
    - Test 8: scaffoldAgent appends to nightshift.yaml agents and schedule arrays when config exists
    - Test 9: scaffoldAgent sets configUpdated=false and warns when nightshift.yaml is missing
    - Test 10: manifest variables include repo_url with empty string default
    - Test 11: manifest beads include clone (git-clone type) and analyze (standard type)
  </behavior>
  <action>
Create `tests/unit/scaffold.test.ts` using vitest.

Test setup: Create a tmpdir before each test. For tests that need nightshift.yaml, write a minimal valid config to `path.join(tmpdir, 'nightshift.yaml')` containing `agents: []` and `schedule: []`.

Import `scaffoldAgent` from `../../src/agent/scaffold.js`. Import `ManifestSchema` from `../../src/agent/manifest-schema.js` for validating the generated manifest. Import `parse as parseYaml` from `yaml`.

For each test:
- Call `scaffoldAgent(name, { base: tmpdir })` (or with force/without config)
- Assert file existence with `fs.access()`
- Read and parse manifest.yaml to verify structure
- Validate manifest against ManifestSchema with `safeParse()`
- Check that nightshift.yaml was modified (parse and inspect agents/schedule arrays)

Clean up tmpdir in afterEach.

Follow the existing test patterns in `tests/unit/` -- use `describe`/`it` blocks, async test functions, `expect` assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/scaffold.test.ts</automated>
  </verify>
  <done>All scaffold unit tests pass. Coverage includes happy path, error cases (invalid name, existing dir), config update, and generated file content validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for agent CLI commands</name>
  <files>tests/integration/agent-commands.test.ts</files>
  <behavior>
    - Test 1: `nightshift agent init test-agent` creates agent directory and exits 0
    - Test 2: `nightshift agent init test-agent` (second time, no --force) exits 1
    - Test 3: `nightshift agent init test-agent --force` succeeds on existing directory
    - Test 4: `nightshift agent validate test-agent` exits 0 after init (scaffolded agent is valid)
    - Test 5: `nightshift agent validate /nonexistent/path` exits 1 with error message
    - Test 6: `nightshift agent list` shows the initialized agent in table output
    - Test 7: `nightshift agent list --json` outputs valid JSON array
    - Test 8: `nightshift agent list` with no agents shows helpful empty message
    - Test 9: `nightshift agent show test-agent` displays manifest summary and bead pipeline
  </behavior>
  <action>
Create `tests/integration/agent-commands.test.ts` using vitest.

Test setup: Create a tmpdir with the nightshift project structure:
- Write a minimal `nightshift.yaml` (minimal YAML string with `agents: []`, `schedule: []`, `agents_dir: ./agents`)
- Create `.nightshift/logs/` directory
- Set `cwd` to tmpdir when spawning CLI

Run CLI commands using `execFile` from `node:child_process` (NOT exec -- use array args to prevent shell injection). Pattern: `execFile('npx', ['tsx', '<project-root>/bin/nightshift.ts', 'agent', 'init', 'test-agent'], { cwd: tmpdir })`. Check existing integration tests in this project for the exact spawn pattern and adapt accordingly.

Wrap execFile in a promise helper that resolves with `{ stdout, stderr, exitCode }`.

For `agent init` tests: verify directory creation, then verify `agent validate` passes on the result.
For `agent list` tests: init an agent first, then list. For empty state, use a fresh tmpdir with config but no agents.
For `agent validate` error case: create an agents dir with a broken manifest (missing required fields).

Assert on: exit code (0 or 1), stdout content (table headers, agent name, etc.), stderr for errors.

Clean up tmpdir in afterEach.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/agent-commands.test.ts</automated>
  </verify>
  <done>All integration tests pass. The scaffolded agent passes validation. List shows correct table output. Error cases produce non-zero exit codes.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/scaffold.test.ts` -- all scaffold unit tests pass
- `npx vitest run tests/integration/agent-commands.test.ts` -- all CLI integration tests pass
- `npx vitest run` -- full suite still green
</verification>

<success_criteria>
- scaffoldAgent unit tests cover happy path, error cases, config update, and file content
- Integration tests cover all 4 subcommands via CLI subprocess
- Scaffolded agent passes validation (init then validate flow)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-developer-experience/11-02-SUMMARY.md`
</output>
