---
phase: 17-e2e-testing-framework
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/e2e/happy-path.test.ts
  - tests/e2e/cli-commands.test.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "A submitted agent runs through the full pipeline and produces an inbox report"
    - "The inbox report contains the agent name, step count, and mock output"
    - "ntfy mock server receives start and success notification HTTP requests"
    - "CLI status command shows running daemon state"
    - "CLI submit command queues a task and returns confirmation"
    - "CLI cancel command cancels a pending task"
    - "CLI inbox command lists completed reports"
  artifacts:
    - path: "tests/e2e/happy-path.test.ts"
      provides: "Full pipeline E2E test: daemon start -> agent submit -> execution -> inbox report -> ntfy verification"
    - path: "tests/e2e/cli-commands.test.ts"
      provides: "CLI command E2E tests: status, submit, cancel, schedule, inbox"
  key_links:
    - from: "tests/e2e/happy-path.test.ts"
      to: ".nightshift/inbox/*.md"
      via: "waitForInboxReport polling"
      pattern: "waitForInboxReport.*happy-path"
    - from: "tests/e2e/happy-path.test.ts"
      to: "ntfy mock server"
      via: "getRequests() after task completion"
      pattern: "getRequests.*length"
    - from: "tests/e2e/cli-commands.test.ts"
      to: "bin/nightshift.ts"
      via: "run() helper CLI subprocess"
      pattern: "run\\(\\[.*\\]"
---

<objective>
Write the happy-path E2E test and CLI command E2E tests.

Purpose: The happy-path test validates the full pipeline end-to-end: daemon picks up a submitted task, runs it through the mock claude shim, generates an inbox report, and sends ntfy notifications. The CLI tests verify that all user-facing commands work correctly against a live daemon.

Output: Two test files covering TEST-02 (happy path) and TEST-03 (CLI commands), all passing.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-e2e-testing-framework/17-CONTEXT.md
@.planning/phases/17-e2e-testing-framework/17-RESEARCH.md
@.planning/phases/17-e2e-testing-framework/17-01-SUMMARY.md

@src/agent/step-runner.ts
@src/inbox/reporter.ts
@src/cli/commands/submit.ts
@src/cli/commands/status.ts
@src/cli/commands/cancel.ts
@src/cli/commands/inbox.ts
@src/cli/commands/schedule.ts
@tests/e2e/helpers/daemon.ts
@tests/e2e/helpers/ntfy-server.ts
@tests/e2e/helpers/config.ts
@tests/e2e/helpers/cli.ts
@tests/e2e/lifecycle.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write happy-path E2E test</name>
  <files>tests/e2e/happy-path.test.ts</files>
  <behavior>
    - Test: submit happy-path-agent, daemon executes it, inbox report appears with correct content
    - Test: inbox report contains agent_name, step_count, status: completed, and mock output text
    - Test: ntfy mock server received at least a start and success notification POST
    - Test: no real claude binary was called (verified by shim being the only claude on PATH)
  </behavior>
  <action>
    Create `tests/e2e/happy-path.test.ts`.

    **Setup (beforeEach):**
    - Create temp directory
    - Start ntfy mock server via `createNtfyMockServer()`
    - Call `writeE2EConfig(tmpDir, { ntfyPort: server.port, agentNames: ["happy-path-agent"] })`
    - Build env with mock-claude PATH prepended
    - Start daemon via `startDaemon(tmpDir, env)`

    **Teardown (afterEach):**
    - `killDaemon(tmpDir)` (safety net)
    - Close ntfy mock server
    - Remove temp directory

    **Test: "full pipeline — submit agent, verify inbox report and ntfy"**
    1. Run `run(["submit", "--agent", "happy-path-agent"], { cwd: tmpDir, env })` — assert exit code 0
    2. Poll for inbox report: create a `waitForInboxReport` helper that reads `{tmpDir}/.nightshift/inbox/` every 300ms until a file matching `/happy-path-agent/` appears (max 30s timeout). This helper can live in this test file or be added to a helpers module.
    3. Read the inbox report content
    4. Assert report contains:
       - `status: completed` (in YAML frontmatter)
       - `agent_name: happy-path-agent`
       - `step_count: 1`
       - `mock success output` (or whatever the success.json response contains)
    5. Check ntfy mock server: `server.getRequests()` should have at least 2 entries (start + success notifications). Verify one contains the agent name.

    **Important details from RESEARCH.md:**
    - The submit command format may be `nightshift submit --agent <name>` for agent-based submissions. Check the actual CLI command signature in `src/cli/commands/submit.ts`.
    - Inbox report filenames follow the pattern `{date}_{sanitizedName}_{shortId}.md` — use a regex pattern, not exact match.
    - The reporter writes YAML frontmatter — parse or use string matching for assertions.
    - The mock claude response file path must be accessible to the shim. The happy-path-agent manifest declares `MOCK_CLAUDE_RESPONSE_FILE` in env — the config helper must resolve the path when copying agents to tmpDir (replace `{{response_file}}` with the actual absolute path to success.json, or copy the response file into a known location).
  </action>
  <verify>
    <automated>npx vitest run --config vitest.e2e.config.ts tests/e2e/happy-path.test.ts</automated>
  </verify>
  <done>
    Happy-path test passes: agent submits, daemon executes via mock shim, inbox report generated with correct metadata and output, ntfy notifications sent to mock server.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write CLI command E2E tests</name>
  <files>tests/e2e/cli-commands.test.ts</files>
  <behavior>
    - Test: `nightshift status` shows running daemon info (PID, uptime, task counts)
    - Test: `nightshift submit --agent happy-path-agent` returns confirmation with task ID
    - Test: `nightshift cancel <taskId>` cancels a pending task before daemon picks it up
    - Test: `nightshift schedule` shows recurring task schedule (even if empty)
    - Test: `nightshift inbox` lists completed reports after a task finishes
    - Test: `nightshift inbox --read <file>` outputs report content
  </behavior>
  <action>
    Create `tests/e2e/cli-commands.test.ts`.

    **Setup (beforeEach):**
    - Create temp directory
    - Start ntfy mock server (optional — only needed if testing notification side effects)
    - Call `writeE2EConfig(tmpDir, { ntfyPort: server.port, agentNames: ["happy-path-agent"] })`
    - Build env with mock-claude PATH prepended
    - Start daemon via `startDaemon(tmpDir, env)`

    **Teardown (afterEach):**
    - `killDaemon(tmpDir)` (safety net)
    - Close ntfy mock server
    - Remove temp directory

    **Test: "status shows running daemon"**
    - Run `run(["status"], { cwd: tmpDir, env })` — assert exit code 0
    - Assert stdout contains the daemon PID or "running"
    - Assert stdout contains uptime or task count info

    **Test: "submit queues a task"**
    - Run `run(["submit", "--agent", "happy-path-agent"], { cwd: tmpDir, env })` — assert exit code 0
    - Assert stdout contains "queued" or a task ID
    - Verify a task file appears in `.nightshift/queue/`

    **Test: "cancel removes a pending task"**
    - Use a very long timeout agent or pause the daemon (set `max_concurrent: 0` in config) to keep a task in pending state
    - Submit a task, capture the task ID from stdout
    - Run `run(["cancel", taskId], { cwd: tmpDir, env })` — assert exit code 0
    - Assert the task file is removed from queue or marked cancelled
    - Alternative approach if cancellation requires specific setup: submit a task, immediately cancel before daemon picks it up (daemon poll_interval_ms is 500ms — there's a window). If this is too flaky, use a separate config with `max_concurrent: 0` or very long poll interval.

    **Test: "schedule shows recurring tasks"**
    - Run `run(["schedule"], { cwd: tmpDir, env })` — assert exit code 0
    - Assert stdout contains schedule info (may be empty if no recurring tasks configured)

    **Test: "inbox lists reports"**
    - Submit happy-path-agent, wait for inbox report via polling
    - Run `run(["inbox"], { cwd: tmpDir, env })` — assert exit code 0
    - Assert stdout lists the report filename

    **Test: "inbox --read shows report content"**
    - After inbox report exists, get its filename
    - Run `run(["inbox", "--read", filename], { cwd: tmpDir, env })` — assert exit code 0
    - Assert stdout contains report content (agent name, status, output)

    **Important:** Check exact CLI command signatures by reading the command source files. The submit command may use `--agent` flag or positional argument. The cancel command needs a task ID.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.e2e.config.ts tests/e2e/cli-commands.test.ts</automated>
  </verify>
  <done>
    All CLI E2E tests pass: status shows daemon info, submit queues tasks, cancel removes pending tasks, schedule shows config, inbox lists and reads reports. All commands tested against a live daemon with no real external calls.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --config vitest.e2e.config.ts tests/e2e/happy-path.test.ts tests/e2e/cli-commands.test.ts` passes
- Inbox report verified with correct metadata fields
- ntfy mock server captures notification payloads
- CLI commands return expected exit codes and output
</verification>

<success_criteria>
- happy-path.test.ts: full pipeline test passes (submit -> execute -> report -> ntfy)
- cli-commands.test.ts: all 6 CLI commands tested (status, submit, cancel, schedule, inbox, inbox --read)
- No real claude binary called, no real ntfy server contacted
- All tests clean up daemon processes and temp dirs
</success_criteria>

<output>
After completion, create `.planning/phases/17-e2e-testing-framework/17-02-SUMMARY.md`
</output>
