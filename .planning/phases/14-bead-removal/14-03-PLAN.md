---
phase: 14-bead-removal
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - tests/unit/engine.test.ts
  - tests/unit/manifest-schema.test.ts
  - tests/unit/manifest-loader.test.ts
  - tests/unit/agent-pool.test.ts
  - tests/unit/startup-validation.test.ts
  - tests/unit/scaffold.test.ts
  - tests/unit/bead-runner.test.ts
  - tests/unit/bead-registry.test.ts
  - tests/unit/standard-bead-plugin.test.ts
  - tests/unit/git-clone-bead-plugin.test.ts
  - tests/unit/git-harness.test.ts
  - tests/unit/mapper.test.ts
  - tests/unit/config.test.ts
  - tests/unit/orchestrator.test.ts
  - tests/unit/reporter.test.ts
  - tests/integration/run.test.ts
  - tests/integration/agent-commands.test.ts
  - tests/integration/submit.test.ts
  - tests/integration/cancel.test.ts
  - tests/integration/status.test.ts
  - src/agent/scaffold.ts
autonomous: true
requirements: [BEAD-03, BEAD-04]

must_haves:
  truths:
    - "All tests pass with npm test"
    - "No test file references deleted modules (bead-plugin, bead-registry, standard-bead-plugin, git-clone-bead-plugin, git-harness, beads/client)"
    - "No source or test file contains 'bead' in any identifier, import, or type name"
    - "Agent scaffold template produces steps-based manifest"
  artifacts:
    - path: "tests/unit/engine.test.ts"
      provides: "Engine tests mocking at spawnWithTimeout level (no plugin mocks)"
      contains: "runStep"
    - path: "tests/unit/step-runner.test.ts"
      provides: "Renamed from bead-runner.test.ts, tests buildStepEnv/buildStepArgs/runStep"
      contains: "buildStepEnv"
    - path: "src/agent/scaffold.ts"
      provides: "Agent scaffold with steps array (no type field, no git-clone step)"
      contains: "steps"
  key_links:
    - from: "tests/unit/engine.test.ts"
      to: "src/agent/engine.ts"
      via: "imports AgentEngine, mocks spawnWithTimeout"
      pattern: "AgentEngine"
    - from: "tests/unit/manifest-schema.test.ts"
      to: "src/agent/manifest-schema.ts"
      via: "imports StepSchema, ManifestSchema"
      pattern: "StepSchema"
---

<objective>
Migrate all test files to step terminology, delete obsolete test files, update the agent scaffold template, and ensure the full test suite passes.

Purpose: Complete the bead removal by ensuring all tests work against the new step-based contracts and no trace of "bead" remains anywhere in the codebase.

Output: Obsolete test files deleted, remaining tests renamed/updated, scaffold generates step-based manifests, `npm test` passes clean.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bead-removal/14-CONTEXT.md
@.planning/phases/14-bead-removal/14-01-SUMMARY.md
@.planning/phases/14-bead-removal/14-02-SUMMARY.md

<interfaces>
<!-- After Plans 01+02, the executor should reference these contracts -->

Engine (from Plan 02):
- AgentEngine constructor: `new AgentEngine(logger: Logger)` (no registry)
- AgentEngine.run(agentDir, agentsRoot, taskId, configOverrides?) -> AgentRunResult
- AgentEngine.dryRun(agentDir, agentsRoot, configOverrides?) -> void
- Internally calls runStep() from step-runner.ts

Types (from Plan 01):
- StepOutcome, StepErrorCategory, AgentPipelineContext, AgentRunResult (with perStep, failedStepIndex, stepOutputs)
- ResolvedStep (no type field), LoadedManifest (steps array)
- StepResult, buildStepEnv, buildStepArgs, runStep
- StepContractViolationError, StepOutputMissingError

Schema (from Plan 01):
- StepSchema (no type field), ManifestSchema (steps array)

Config (from Plan 01):
- NightShiftConfig has no beads field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete tests, rename and update remaining tests</name>
  <files>
    tests/unit/bead-registry.test.ts,
    tests/unit/standard-bead-plugin.test.ts,
    tests/unit/git-clone-bead-plugin.test.ts,
    tests/unit/git-harness.test.ts,
    tests/unit/mapper.test.ts,
    tests/unit/bead-runner.test.ts,
    tests/unit/engine.test.ts,
    tests/unit/manifest-schema.test.ts,
    tests/unit/manifest-loader.test.ts,
    tests/unit/agent-pool.test.ts,
    tests/unit/startup-validation.test.ts,
    tests/unit/config.test.ts,
    tests/unit/orchestrator.test.ts,
    tests/unit/reporter.test.ts,
    tests/integration/run.test.ts,
    tests/integration/agent-commands.test.ts,
    tests/integration/submit.test.ts,
    tests/integration/cancel.test.ts,
    tests/integration/status.test.ts
  </files>
  <action>
    **DELETE these test files (modules no longer exist):**
    - tests/unit/bead-registry.test.ts
    - tests/unit/standard-bead-plugin.test.ts
    - tests/unit/git-clone-bead-plugin.test.ts
    - tests/unit/git-harness.test.ts
    - tests/unit/mapper.test.ts (beads mapper gone)

    **RENAME tests/unit/bead-runner.test.ts -> tests/unit/step-runner.test.ts:**
    - Create new file, delete old
    - Update all imports: bead-runner -> step-runner
    - Rename all test descriptions and assertions: buildBeadEnv -> buildStepEnv, buildBeadArgs -> buildStepArgs, runBead -> runStep, BeadResult -> StepResult
    - Rename variable names: beadName -> stepName

    **UPDATE tests/unit/engine.test.ts:**
    - Remove any imports of BeadRegistry, BeadPlugin, StandardBeadPlugin, GitCloneBeadPlugin
    - Engine is now constructed with just logger: `new AgentEngine(logger)`
    - Mock at the spawnWithTimeout level (vi.mock("../src/utils/process.ts")) to verify the full chain: prompt rendering -> arg building -> env building -> spawn
    - Rename all bead -> step in variable names, test descriptions, assertions
    - perBead -> perStep, failedBeadIndex -> failedStepIndex, beadOutputs -> stepOutputs
    - Test manifests should use `steps` array with no `type` field

    **UPDATE tests/unit/manifest-schema.test.ts:**
    - BeadSchema -> StepSchema in imports and tests
    - ManifestSchema now expects `steps` key
    - Step definitions have no `type` field -- update all test fixtures
    - Duplicate name validation message: "bead" -> "step"
    - retryFrom validation: update message expectations

    **UPDATE tests/unit/manifest-loader.test.ts:**
    - Update test manifest fixtures: `beads` array -> `steps` array, remove `type` field from entries
    - Rename function references: validateBeadOutput -> validateStepOutput, resolveBeadConfig -> resolveStepConfig (if tested directly)
    - Error message expectations: "bead" -> "step"
    - LoadedManifest assertions: .beads -> .steps

    **UPDATE tests/unit/agent-pool.test.ts:**
    - Remove registry/plugin creation mocks
    - Engine construction should use `new AgentEngine(logger)` pattern
    - Rename perBead -> perStep in mock results

    **UPDATE tests/unit/startup-validation.test.ts:**
    - Update manifest fixtures to use `steps` array
    - Update error message expectations from "bead" to "step"

    **UPDATE tests/unit/config.test.ts:**
    - Remove any test cases for `beads.enabled` config field
    - Remove beads from test config fixtures
    - Default config YAML should not contain beads section

    **UPDATE tests/unit/orchestrator.test.ts:**
    - Remove BeadsClient mocking
    - Remove tests for beads-enabled code paths
    - File-queue is the only path -- simplify accordingly
    - Remove `beadsEnabled` from log assertions if present

    **UPDATE tests/unit/reporter.test.ts:**
    - beadCount -> stepCount in any report assertions

    **UPDATE integration tests (run.test.ts, agent-commands.test.ts, submit.test.ts, cancel.test.ts, status.test.ts):**
    - Remove beads-related test paths
    - Update manifest fixtures in agent-commands.test.ts to use `steps` array
    - Update any config fixtures to remove beads section
    - Rename bead -> step in assertions and test descriptions

    **Key pattern for the 4 GITLAB_TOKEN isolation tests** (currently in bead-runner.test.ts, now step-runner.test.ts):
    - Keep these tests, rename buildBeadEnv -> buildStepEnv
    - These verify that GITLAB_TOKEN is only forwarded when explicitly declared -- critical security tests
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>All deleted test files gone. All remaining tests updated with step terminology. npm test passes with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Update scaffold template and final bead-word sweep</name>
  <files>
    src/agent/scaffold.ts
  </files>
  <action>
    **src/agent/scaffold.ts:**
    - Replace the manifest template's `beads` array with `steps` array
    - Remove the git-clone step entirely from the scaffold template (per CONTEXT.md: agents handle repo cloning in their prompt steps)
    - Remove the `type` field from step definitions
    - The first step should be a standard prompt step (e.g. "analyze") -- include git clone instructions in the prompt text instead
    - Remove `variables: { repo_url: "" }` from manifest (repo cloning is now agent prompt responsibility)
    - Update the config registration (schedule entry) to not include repo_url variable
    - Update the next-steps message to reference "steps" not "beads"

    **Final sweep -- grep the ENTIRE codebase for any remaining "bead" references:**
    - Run: `grep -rn "bead\|Bead\|BEAD" src/ tests/ --include="*.ts" | grep -v node_modules`
    - For each hit, rename to "step"/"Step" equivalent
    - Acceptable exceptions: none. The word "bead" should not appear in ANY .ts file.
    - Also check: `grep -rn "bead\|Bead" src/ tests/ --include="*.md"` for any prompt files or documentation
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && echo "---" && grep -rn "bead\|Bead\|BEAD" src/ tests/ --include="*.ts" | grep -v node_modules | wc -l | xargs test 0 -eq && echo "No bead references" || echo "BEAD REFERENCES REMAIN"</automated>
  </verify>
  <done>Scaffold generates step-based manifests with no git-clone step. Zero occurrences of "bead" in any .ts source or test file. npm test passes clean.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm test` passes with zero failures
- `grep -rn "bead\|Bead\|BEAD" src/ tests/ --include="*.ts" | grep -v node_modules` returns zero lines
- `ls tests/unit/bead-registry.test.ts tests/unit/standard-bead-plugin.test.ts tests/unit/git-clone-bead-plugin.test.ts tests/unit/git-harness.test.ts tests/unit/mapper.test.ts tests/unit/bead-runner.test.ts 2>&1 | grep "No such file"` confirms all 6 files deleted
- `test -f tests/unit/step-runner.test.ts` confirms rename
- `npx tsc --noEmit` passes clean
</verification>

<success_criteria>
- npm test passes with all tests green
- npx tsc --noEmit passes with no errors
- Zero "bead" occurrences in any .ts file across src/ and tests/
- 5 obsolete test files deleted, bead-runner.test.ts renamed to step-runner.test.ts
- Scaffold template produces steps-based manifest (no type field, no git-clone step)
- GITLAB_TOKEN isolation tests preserved (renamed to buildStepEnv)
</success_criteria>

<output>
After completion, create `.planning/phases/14-bead-removal/14-03-SUMMARY.md`
</output>
