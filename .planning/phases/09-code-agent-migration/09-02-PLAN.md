---
phase: 09-code-agent-migration
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - agents/code-agent/manifest.yaml
  - agents/code-agent/prompts/analyze.md
  - agents/code-agent/prompts/implement.md
  - agents/code-agent/prompts/verify.md
  - agents/code-agent/prompts/mr.md
  - agents/code-agent/prompts/log.md
  - tests/unit/code-agent-manifest.test.ts
autonomous: true
requirements:
  - MIGR-01

must_haves:
  truths:
    - "agents/code-agent/manifest.yaml loads successfully through AgentEngine.dryRun()"
    - "Running the code-agent through AgentEngine with mocked runBead produces MR_CREATED outcome shape"
    - "Running the code-agent through AgentEngine with mocked analyze returning NO_IMPROVEMENT produces a result where beadOutputs['analyze'].result === 'NO_IMPROVEMENT'"
    - "All prompt templates resolve without undefined variable errors when provided with manifest variables and config overrides"
    - "The code-agent directory can be loaded from a different agentsRoot without engine code changes"
    - "Prompts use {{beads.*}} template variables instead of {{handoff_file}} or {{analysis_file}}"
    - "Prompts output JSON code blocks to stdout instead of writing to files"
  artifacts:
    - path: "agents/code-agent/manifest.yaml"
      provides: "Full 6-bead pipeline declaration (clone, analyze, implement, verify, mr, log)"
    - path: "agents/code-agent/prompts/analyze.md"
      provides: "Analyze prompt adapted for JSON code block output"
    - path: "agents/code-agent/prompts/implement.md"
      provides: "Implement prompt using {{beads.analyze.output.*}} for analysis data"
    - path: "agents/code-agent/prompts/verify.md"
      provides: "Verify prompt with JSON code block output and non-zero exit on failure"
    - path: "agents/code-agent/prompts/mr.md"
      provides: "MR prompt using {{beads.analyze.output.*}} for description"
    - path: "agents/code-agent/prompts/log.md"
      provides: "Log prompt using {{beads.*}} for run record data"
    - path: "tests/unit/code-agent-manifest.test.ts"
      provides: "Integration test verifying manifest loads and pipeline runs with mocked beads"
  key_links:
    - from: "agents/code-agent/manifest.yaml"
      to: "src/agent/engine.ts"
      via: "loadManifest → AgentEngine.run()"
      pattern: "beads.*type.*standard"
    - from: "agents/code-agent/prompts/*.md"
      to: "src/agent/template.ts"
      via: "renderAgentTemplate resolves {{beads.*}} and {{variable}} patterns"
      pattern: "\\{\\{beads\\."
    - from: "tests/unit/code-agent-manifest.test.ts"
      to: "agents/code-agent/"
      via: "Real agent dir loaded by mocked engine"
      pattern: "agents/code-agent"
---

<objective>
Create the `agents/code-agent/` directory with manifest.yaml and 5 adapted prompt files, then validate with an integration test that runs the full pipeline through AgentEngine with mocked beads.

Purpose: This is the core deliverable of Phase 9 — the code-agent exists as a self-contained directory runnable by `AgentEngine` with outcome shape parity to v1.0.

Output: Agent directory with manifest + prompts, integration test proving pipeline runs correctly.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-code-agent-migration/09-CONTEXT.md
@.planning/phases/09-code-agent-migration/09-RESEARCH.md
@.planning/phases/09-code-agent-migration/09-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/agent/manifest-schema.ts (after Plan 01 extensions):
```typescript
export const BeadSchema = z.object({
  name: z.string().min(1),
  type: z.string().min(1),
  prompt: z.string().min(1),
  model: z.string().optional(),
  allowedTools: z.array(z.string()).optional(),
  env: z.array(EnvVarSchema).optional(),
  timeout: z.string().optional(),
  mcpConfig: z.string().optional(),    // Plan 01 addition
  retry: RetrySchema.optional(),       // Plan 01 addition
  outputSchema: z.record(z.string(), z.unknown()),
}).strict();
```

From src/agent/manifest-types.ts (after Plan 01 extensions):
```typescript
export interface ResolvedBead {
  name: string;
  type: string;
  prompt: string;
  model: string;
  timeout: string;
  allowedTools: string[];
  env: ResolvedEnvVar[];
  outputSchema: Record<string, unknown>;
  compiledOutputSchema: z.ZodTypeAny;
  mcpConfig?: string;                   // Plan 01 addition
  retry?: { maxAttempts: number; retryFrom: string };  // Plan 01 addition
}
```

From src/agent/engine-types.ts (after Plan 01 extensions):
```typescript
export interface AgentRunResult<T = unknown> {
  runId: string;
  agentName: string;
  status: PipelineStatus;
  finalOutput: T | null;
  perBead: BeadOutcome[];
  totalDurationMs: number;
  failedBeadIndex?: number;
  errorCategory?: BeadErrorCategory;
  suggestedDelayMs?: number;
  error?: string;
  beadOutputs?: Record<string, unknown>;  // Plan 01 addition
}
```

From src/agent/template.ts:
```typescript
export const BUILT_IN_VARS = ["task_id", "run_date", "agent_name", "repo_path"] as const;
// Built-ins are injected automatically by the engine. Prompts can reference {{task_id}}, {{run_date}}, etc.
// Bead outputs accessible via {{beads.bead_name.output.field}}
// validateTemplateVars skips beads.* paths at load time
```

From src/agent/engine.ts:
```typescript
// AgentEngine.run(agentDir, agentsRoot, taskId, configOverrides)
// AgentEngine.dryRun(agentDir, agentsRoot, configOverrides)
// configOverrides merge with manifest.variables (config wins)
```

v1.0 variable mapping (from RESEARCH.md):
| v1.0 variable | Manifest equivalent | Source |
|---|---|---|
| {{date}} | {{run_date}} | Built-in |
| {{repo_url}} | {{repo_url}} | Manifest variable |
| {{category}} | {{category}} | Config override |
| {{category_guidance}} | {{category_guidance}} | Config override |
| {{allowed_commands}} | {{allowed_commands}} | Manifest variable |
| {{reviewer}} | {{reviewer}} | Manifest variable |
| {{handoff_file}} | REMOVED — use JSON code block output |
| {{analysis_file}} | REMOVED — use {{beads.analyze.output.*}} |
| {{verify_error}} | {{retry_error}} | Injected by engine on retry |
| {{build_commands}} | {{allowed_commands}} | Same variable |
| {{short_description}} | Derived from {{beads.analyze.output.selected.description}} |
| {{confluence_page_id}} | {{confluence_page_id}} | Manifest variable |
| {{mr_url}} | {{beads.mr.output.mr_url}} | Previous bead output |
| {{cost_usd}} / {{duration_seconds}} / {{summary}} | Computed by log prompt from bead context |

v1.0 prompts location: src/agent/prompts/{analyze,implement,verify,mr,log}.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agents/code-agent/ directory with manifest.yaml and adapted prompt files</name>
  <files>
    agents/code-agent/manifest.yaml
    agents/code-agent/prompts/analyze.md
    agents/code-agent/prompts/implement.md
    agents/code-agent/prompts/verify.md
    agents/code-agent/prompts/mr.md
    agents/code-agent/prompts/log.md
  </files>
  <action>
Create the `agents/code-agent/` directory and `agents/code-agent/prompts/` subdirectory.

**manifest.yaml:**

Write `agents/code-agent/manifest.yaml` with the following structure:

```yaml
name: code-agent
description: Analyzes a repository nightly and creates a focused improvement MR

model: claude-opus-4-6
timeout: 30m
allowedTools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep

variables:
  repo_url: ""
  category: ""
  category_guidance: ""
  confluence_page_id: ""
  mcp_config_path: ""
  reviewer: ""
  allowed_commands: "git, glab"

beads:
  - name: clone
    type: git-clone
    prompt: prompts/clone-stub.md
    env:
      - GITLAB_TOKEN
    outputSchema:
      type: object
      properties:
        repoDir:
          type: string
        handoffDir:
          type: string
      required:
        - repoDir
        - handoffDir

  - name: analyze
    type: standard
    prompt: prompts/analyze.md
    model: claude-opus-4-6
    outputSchema:
      type: object
      properties:
        result:
          type: string
          enum:
            - IMPROVEMENT_FOUND
            - NO_IMPROVEMENT
        categoryUsed:
          type: string
        reason:
          type: string
        candidates:
          type: array
          items:
            type: object
            properties:
              rank:
                type: integer
              files:
                type: array
                items:
                  type: string
              description:
                type: string
              rationale:
                type: string
        selected:
          type: object
          properties:
            rank:
              type: integer
            files:
              type: array
              items:
                type: string
            description:
              type: string
            rationale:
              type: string
      required:
        - result
        - categoryUsed

  - name: implement
    type: standard
    prompt: prompts/implement.md
    model: claude-opus-4-6
    outputSchema:
      type: object
      properties:
        status:
          type: string
          enum:
            - IMPLEMENTED
      required:
        - status

  - name: verify
    type: standard
    prompt: prompts/verify.md
    model: claude-sonnet-4-6
    retry:
      maxAttempts: 3
      retryFrom: implement
    outputSchema:
      type: object
      properties:
        passed:
          type: boolean
        error_details:
          type: string
      required:
        - passed

  - name: mr
    type: standard
    prompt: prompts/mr.md
    model: claude-sonnet-4-6
    env:
      - GITLAB_TOKEN
    outputSchema:
      type: object
      properties:
        mr_url:
          type: string
        outcome:
          type: string
          enum:
            - MR_CREATED
            - MR_FAILED
      required:
        - outcome

  - name: log
    type: standard
    prompt: prompts/log.md
    model: claude-sonnet-4-6
    timeout: 2m
    mcpConfig: "{{mcp_config_path}}"
    allowedTools:
      - mcp__atlassian__getAccessibleAtlassianResources
      - mcp__atlassian__getConfluencePage
      - mcp__atlassian__updateConfluencePage
      - Read
    outputSchema:
      type: object
      properties:
        logged:
          type: boolean
      required:
        - logged
```

Note: The clone bead needs a minimal prompt stub file. Create `agents/code-agent/prompts/clone-stub.md` with just: `Git clone bead — handled by GitCloneBeadPlugin. This prompt is not used.`

**prompts/analyze.md** — Adapt from `src/agent/prompts/analyze.md`:
- Replace `{{date}}` with `{{run_date}}`
- Replace `{{allowed_commands}}` reference (same name, works as-is)
- REMOVE all references to `{{handoff_file}}` — instead, the output section instructs the agent to output a JSON code block to stdout (not write to a file)
- Keep the same constraints, process, and category guidance sections
- The output JSON structure stays the same but the instruction changes from "Write the following JSON to the file at path `{{handoff_file}}`" to "Output the following JSON code block and then stop. Do not write any files."
- Keep the `"category_used"` field name as `"categoryUsed"` (camelCase) to match the outputSchema in the manifest

**prompts/implement.md** — Adapt from `src/agent/prompts/implement.md`:
- Replace `{{date}}` with `{{run_date}}`
- Replace `{{analysis_file}}` references with inline instructions: "The analysis selected the following improvement:" followed by template variables from the analyze bead output:
  - Description: `{{beads.analyze.output.selected.description}}`
  - Files to modify: `{{beads.analyze.output.selected.files}}`
  - Rationale: `{{beads.analyze.output.selected.rationale}}`
- Replace `{{verify_error}}` with a conditional block:
  ```
  {{retry_error}}
  ```
  The engine injects `retry_error` on retry. When empty string (first attempt), the template renders an empty string. When populated (retry), it contains the verify error details. Wrap it in context: "If the following retry error is present, fix the issues described before re-implementing:\n\n{{retry_error}}"
- Add output section: "When complete, output the following JSON code block:" with `{ "status": "IMPLEMENTED" }`. The implement bead's main job is modifying files, but it must also produce this JSON confirmation to stdout.

**prompts/verify.md** — Adapt from `src/agent/prompts/verify.md`:
- Replace `{{build_commands}}` with `{{allowed_commands}}`
- REMOVE all references to `{{handoff_file}}`
- Output section: "Output the following JSON code block:" with the passed/error_details structure
- The verify bead outputs to stdout (JSON code block), not to a file
- Keep the instruction to NOT modify any source files

**prompts/mr.md** — Adapt from `src/agent/prompts/mr.md`:
- Replace `{{date}}` with `{{run_date}}`
- Replace `{{analysis_file}}` references with template variables from analyze bead output:
  - `{{beads.analyze.output.selected.description}}` for the MR title description
  - `{{beads.analyze.output.candidates}}` for the candidates considered section
  - `{{beads.analyze.output.selected.rationale}}` for why this was selected
- Replace `{{short_description}}` derivation: use `{{beads.analyze.output.selected.description}}` directly in the branch naming instruction (tell the agent to slugify it)
- Add output section: "After creating the MR, output the following JSON code block:" with `{ "outcome": "MR_CREATED", "mr_url": "<the MR URL>" }`. If MR creation fails: `{ "outcome": "MR_FAILED", "mr_url": "" }`

**prompts/log.md** — Adapt from `src/agent/prompts/log.md`:
- Replace `{{date}}` with `{{run_date}}`
- Replace `{{category}}` with `{{beads.analyze.output.categoryUsed}}`
- Replace `{{mr_url}}` with `{{beads.mr.output.mr_url}}`
- Replace `{{cost_usd}}` — this is no longer directly available as a single variable. Instruct the log bead: "Cost and duration are not available as direct variables. Record 'N/A' for cost and duration fields, or derive from the run context if available."
- Replace `{{duration_seconds}}` — same treatment as cost_usd
- Replace `{{summary}}` — derive from `{{beads.analyze.output.selected.description}}`
- Replace `{{confluence_page_id}}` (stays the same — it's a manifest variable)
- Add output section: "After updating the Confluence page, output the following JSON code block:" with `{ "logged": true }`. On failure: `{ "logged": false }`

**Key principles for all prompts:**
- NO references to `{{handoff_file}}` or `{{analysis_file}}` anywhere
- All beads output JSON code blocks to stdout (the engine captures and validates them)
- The implement bead is special: it modifies repo files AND outputs a status JSON
- Use `{{beads.bead_name.output.field}}` for cross-bead data access
- Keep the same level of detail and quality as the v1.0 prompts
  </action>
  <verify>
    <automated>cd /Users/julienderay/code/night-shift && ls agents/code-agent/manifest.yaml agents/code-agent/prompts/analyze.md agents/code-agent/prompts/implement.md agents/code-agent/prompts/verify.md agents/code-agent/prompts/mr.md agents/code-agent/prompts/log.md agents/code-agent/prompts/clone-stub.md && echo "All files exist"</automated>
  </verify>
  <done>
- `agents/code-agent/manifest.yaml` exists with 6-bead pipeline
- All 6 prompt files exist in `agents/code-agent/prompts/`
- Prompts use `{{beads.*}}` template variables, not `{{handoff_file}}`/`{{analysis_file}}`
- Prompts output JSON code blocks to stdout, not to files
- Manifest declares correct models, tools, env, timeouts, and output schemas
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test — run code-agent through AgentEngine with mocked beads</name>
  <files>
    tests/unit/code-agent-manifest.test.ts
  </files>
  <action>
Create `tests/unit/code-agent-manifest.test.ts` that validates the code-agent directory works end-to-end with the engine.

**Setup:**
- Import AgentEngine, BeadRegistry, StandardBeadPlugin, GitCloneBeadPlugin from their modules
- Import the mock helpers from vitest (`vi.mock`, `vi.fn`)
- Mock `src/agent/bead-runner.ts` to intercept `runBead` calls
- Mock `src/agent/git-harness.ts` to intercept `cloneRepo` calls
- Create a `silentLogger` helper that returns a Logger with no-op methods
- Set `process.env.GITLAB_TOKEN = "test-token"` in beforeAll, clean up in afterAll
- The `agentsRoot` is `path.resolve("agents")` and `agentDir` is `path.join(agentsRoot, "code-agent")`

**Helper: makeBeadResult(stdout)**
Returns a BeadResult-shaped object: `{ exitCode: 0, stdout, stderr: "", durationMs: 100, costUsd: 0.01, timedOut: false }`

**Test 1: "manifest loads and passes dryRun validation"**
- Create a BeadRegistry with "standard" and "git-clone" types registered
- Create an AgentEngine instance
- Call `engine.dryRun(agentDir, agentsRoot, { repo_url: "git@gitlab.com:team/repo.git", category: "refactoring", category_guidance: "Broad scope", confluence_page_id: "12345", mcp_config_path: "/tmp/mcp.json", reviewer: "jdoe" })`
- Assert it does not throw (all template vars resolve, all beads registered, all prompt files exist)

**Test 2: "pipeline produces MR_CREATED outcome shape"**
- Mock runBead to return appropriate JSON code block outputs for each bead:
  - analyze: `{ "result": "IMPROVEMENT_FOUND", "categoryUsed": "refactoring", "reason": "Found code duplication", "selected": { "rank": 1, "files": ["src/Foo.scala"], "description": "Remove duplicated validation logic", "rationale": "DRY principle" } }`
  - implement: `{ "status": "IMPLEMENTED" }`
  - verify: `{ "passed": true, "error_details": "" }`
  - mr: `{ "outcome": "MR_CREATED", "mr_url": "https://gitlab.com/team/repo/-/merge_requests/42" }`
  - log: `{ "logged": true }`
- Mock cloneRepo to return `{ repoDir: "/tmp/repo", handoffDir: "/tmp/handoff" }`
- Run `engine.run(agentDir, agentsRoot, "test-task-01", { repo_url: "git@gitlab.com:team/repo.git", category: "refactoring", category_guidance: "Broad scope", confluence_page_id: "12345", mcp_config_path: "/tmp/mcp.json" })`
- Assert `result.status === "SUCCESS"`
- Assert `result.beadOutputs?.mr` matches `{ outcome: "MR_CREATED", mr_url: expect.stringContaining("merge_requests") }`
- Assert `result.beadOutputs?.analyze` matches `{ result: "IMPROVEMENT_FOUND" }` (partial match)
- Assert `result.finalOutput` matches `{ logged: true }`

**Test 3: "pipeline with NO_IMPROVEMENT analyze output"**
- Mock analyze bead to return `{ "result": "NO_IMPROVEMENT", "categoryUsed": "refactoring", "reason": "All files recently modified" }`
- The engine will still try to run implement, verify, mr, log after analyze (the engine does NOT stop on NO_IMPROVEMENT — that's the caller's job to inspect beadOutputs). Mock remaining beads appropriately.
- Actually — reconsider. With NO_IMPROVEMENT, the implement bead may produce invalid output or the prompt may not make sense. But the engine doesn't know about NO_IMPROVEMENT semantics — it just runs beads in order. In practice, the caller (Phase 10 daemon) will check `beadOutputs.analyze.result` and decide whether to abort. For this test, just verify that the engine runs all beads and returns beadOutputs with the analyze output accessible.
- Assert `result.beadOutputs?.analyze` matches `{ result: "NO_IMPROVEMENT", categoryUsed: "refactoring" }`

**Test 4: "agent directory is portable — works from different agentsRoot"**
- Copy the `agents/code-agent/` directory to a temporary directory (using fs.cp recursive)
- Create a new agentsRoot pointing to the temp dir
- Run dryRun with the temp dir path
- Assert it does not throw
- Clean up temp dir

**Test 5: "verify retry triggers re-run of implement bead"**
- Mock verify to return `{ "passed": false, "error_details": "tests fail" }` on first call, `{ "passed": true }` on second call
- Mock implement to return `{ "status": "IMPLEMENTED" }` on both calls
- Also mock spawnWithTimeout (for git reset) to succeed
- Assert the implement bead's runBead was called twice (initial + retry)
- Assert result.status === "SUCCESS"

**Important mocking notes:**
- Each mock of runBead needs to wrap the JSON output in a Claude JSON output format. The `runBead` function returns `BeadResult` with `stdout` containing the raw Claude output. The engine's `StandardBeadPlugin` returns `{ rawOutput: result.stdout }`, and `validateBeadOutput` extracts the last JSON code block. So the mock stdout should be a string containing a JSON code block: `` "```json\n" + JSON.stringify(output) + "\n```" ``
- The `cloneRepo` mock returns `{ repoDir, handoffDir }` directly — the `GitCloneBeadPlugin` calls `cloneRepo()` and wraps the result as JSON in rawOutput.
  </action>
  <verify>
    <automated>cd /Users/julienderay/code/night-shift && npm test -- --reporter=dot tests/unit/code-agent-manifest.test.ts</automated>
  </verify>
  <done>
- dryRun validates the real agent directory without errors
- Full pipeline with mocked beads returns SUCCESS + MR_CREATED outcome shape
- beadOutputs accessible for intermediate results (analyze, mr)
- NO_IMPROVEMENT case handled — beadOutputs contain analyze result for caller inspection
- Agent directory portable — works from a different location
- Verify retry triggers re-run of implement bead
- All integration tests pass
  </done>
</task>

</tasks>

<verification>
1. `ls agents/code-agent/manifest.yaml agents/code-agent/prompts/*.md` — all 7 files exist
2. `npm test -- --reporter=dot tests/unit/code-agent-manifest.test.ts` — all integration tests pass
3. `npm test -- --reporter=dot` — full suite green, no regressions
4. `grep '{{handoff_file}}\|{{analysis_file}}' agents/code-agent/prompts/*.md` returns zero results (old variables eliminated)
5. `grep '{{beads\.' agents/code-agent/prompts/*.md` returns matches in implement, mr, and log prompts (new variable pattern in use)
</verification>

<success_criteria>
- `agents/code-agent/manifest.yaml` declares the full 6-bead pipeline (clone, analyze, implement, verify, mr, log)
- All prompt files exist and use the manifest template variable system
- Integration test proves the pipeline runs through AgentEngine with correct outcome shapes
- beadOutputs expose intermediate results for caller-level decision making (NO_IMPROVEMENT detection)
- Agent directory is portable — no hardcoded paths or engine-specific assumptions
- Zero regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-code-agent-migration/09-02-SUMMARY.md`
</output>
