---
phase: 17-e2e-testing-framework
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/e2e/fixtures/agents/multi-step-failure-agent/manifest.yaml
  - tests/e2e/fixtures/agents/multi-step-failure-agent/prompts/step1.md
  - tests/e2e/fixtures/agents/multi-step-failure-agent/prompts/step2.md
  - tests/e2e/fixtures/agents/retry-agent/manifest.yaml
  - tests/e2e/fixtures/agents/retry-agent/prompts/work.md
  - tests/e2e/fixtures/agents/retry-agent/prompts/review.md
  - tests/e2e/fixtures/agents/timeout-agent/manifest.yaml
  - tests/e2e/fixtures/agents/timeout-agent/prompts/run.md
  - tests/e2e/fixtures/mock-claude/responses/failure.json
  - tests/e2e/fixtures/mock-claude/responses/retry-fail.json
  - tests/e2e/error-scenarios.test.ts
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "An agent that returns status:FAILED produces a report showing failed status"
    - "An agent with a very short timeout produces a report showing timed-out status"
    - "A retry-agent exhausts max retry attempts, then the engine continues past the failing step"
    - "Each error scenario produces a distinct inbox report with correct error information"
  artifacts:
    - path: "tests/e2e/fixtures/agents/multi-step-failure-agent/manifest.yaml"
      provides: "2-step agent where step1 returns status:FAILED"
    - path: "tests/e2e/fixtures/agents/retry-agent/manifest.yaml"
      provides: "2-step agent with retry config — review step returns passed:false"
    - path: "tests/e2e/fixtures/agents/timeout-agent/manifest.yaml"
      provides: "Agent with 2s timeout — mock shim sleeps 10s to trigger timeout"
    - path: "tests/e2e/fixtures/mock-claude/responses/failure.json"
      provides: "Canned response with status:FAILED for semantic failure testing"
    - path: "tests/e2e/fixtures/mock-claude/responses/retry-fail.json"
      provides: "Canned response with passed:false for retry logic testing"
    - path: "tests/e2e/error-scenarios.test.ts"
      provides: "E2E tests for agent failure, timeout, and retry exhaustion"
  key_links:
    - from: "tests/e2e/error-scenarios.test.ts"
      to: "src/agent/engine.ts"
      via: "semantic failure (status:FAILED) and retry (passed:false) code paths"
      pattern: "status.*FAILED|passed.*false"
    - from: "tests/e2e/fixtures/agents/timeout-agent/manifest.yaml"
      to: "src/agent/step-runner.ts"
      via: "timeoutMs triggers spawnWithTimeout kill"
      pattern: "timeout.*2s"
---

<objective>
Write E2E tests for error scenarios: agent failure, timeout, and retry exhaustion.

Purpose: These tests verify that the daemon correctly handles non-happy-path situations — a semantically failing agent, a timed-out agent, and a retry-exhaustion agent. Each scenario creates dedicated fixture agents with mock responses designed to trigger specific error paths in the engine.

Output: Three fixture agents (multi-step-failure, retry, timeout), their response files, and a comprehensive error-scenarios.test.ts covering all error paths.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-e2e-testing-framework/17-CONTEXT.md
@.planning/phases/17-e2e-testing-framework/17-RESEARCH.md
@.planning/phases/17-e2e-testing-framework/17-01-SUMMARY.md

@src/agent/engine.ts
@src/agent/step-runner.ts
@src/agent/manifest-types.ts
@src/inbox/reporter.ts
@tests/e2e/helpers/daemon.ts
@tests/e2e/helpers/ntfy-server.ts
@tests/e2e/helpers/config.ts
@tests/e2e/helpers/cli.ts
@tests/e2e/lifecycle.test.ts

<interfaces>
<!-- Engine error handling from src/agent/engine.ts -->
<!-- Semantic failure: step output has status === "FAILED" → engine returns FATAL result -->
<!-- Retry: step output has passed === false + step.retry config → retries up to maxAttempts -->
<!-- Timeout: spawnWithTimeout kills process → engine returns timeout error -->

From src/agent/manifest-types.ts:
```typescript
export interface StepDefinition {
  name: string;
  prompt: string;
  timeout: string;
  outputSchema: Record<string, unknown>;
  compiledOutputSchema: z.ZodTypeAny;
  mcpConfig?: string;
  retry?: { maxAttempts: number; retryFrom: string };
}
```

From src/agent/step-runner.ts:
```typescript
export interface StepResult {
  stdout: string;
  stderr: string;
  exitCode: number | null;
  timedOut: boolean;
  durationMs: number;
  costUsd: number;
  parsed: unknown;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error scenario fixture agents and response files</name>
  <files>
    tests/e2e/fixtures/agents/multi-step-failure-agent/manifest.yaml,
    tests/e2e/fixtures/agents/multi-step-failure-agent/prompts/step1.md,
    tests/e2e/fixtures/agents/multi-step-failure-agent/prompts/step2.md,
    tests/e2e/fixtures/agents/retry-agent/manifest.yaml,
    tests/e2e/fixtures/agents/retry-agent/prompts/work.md,
    tests/e2e/fixtures/agents/retry-agent/prompts/review.md,
    tests/e2e/fixtures/agents/timeout-agent/manifest.yaml,
    tests/e2e/fixtures/agents/timeout-agent/prompts/run.md,
    tests/e2e/fixtures/mock-claude/responses/failure.json,
    tests/e2e/fixtures/mock-claude/responses/retry-fail.json
  </files>
  <action>
    **multi-step-failure-agent** — 2-step agent where step1 produces a semantic failure:
    - `manifest.yaml`: name: multi-step-failure-agent, 2 steps (step1, step2)
    - step1: outputSchema includes `{ status: string, error: string }`, env declares `MOCK_CLAUDE_RESPONSE_FILE` pointing to `failure.json` absolute path (resolve at fixture copy time, same pattern as happy-path-agent)
    - step2: normal step (should never execute since step1 fails with FATAL)
    - Both prompts are minimal placeholder text

    **failure.json** — Canned semantic failure response:
    ```json
    {"session_id":"mock-session","result":"```json\n{\"status\":\"FAILED\",\"error\":\"mock failure: step1 encountered an error\"}\n```","is_error":false,"duration_ms":100,"total_cost_usd":0,"num_turns":1}
    ```
    Note: `is_error: false` because the claude process itself succeeded — the failure is semantic (in the step output JSON).

    **retry-agent** — 2-step agent where step2 (review) has retry config:
    - `manifest.yaml`: name: retry-agent, 2 steps (work, review)
    - step "work": normal step, env points to `success.json` (from plan 01)
    - step "review": has `retry: { maxAttempts: 2, retryFrom: "work" }`, outputSchema includes `{ passed: boolean, error_details: string }`, env points to `retry-fail.json`
    - When review returns `passed: false`, engine retries from "work" step up to maxAttempts times, then continues past review

    **retry-fail.json** — Canned retry-fail response:
    ```json
    {"session_id":"mock-session","result":"```json\n{\"passed\":false,\"error_details\":\"validation check failed\"}\n```","is_error":false,"duration_ms":100,"total_cost_usd":0,"num_turns":1}
    ```

    **timeout-agent** — Single-step agent with very short timeout:
    - `manifest.yaml`: name: timeout-agent, 1 step (run), timeout: "2s"
    - env declares `MOCK_CLAUDE_SLEEP_MS` with value `"10000"` (10 seconds — longer than the 2s timeout)
    - `MOCK_CLAUDE_SLEEP_MS` must be declared in the manifest env block so it passes through `buildStepEnv`
    - Do NOT set `MOCK_CLAUDE_RESPONSE_FILE` — the shim will sleep and get killed before it can respond
    - Prompt is minimal placeholder text

    **Important: MOCK_CLAUDE_SLEEP_MS env propagation** — Same as MOCK_CLAUDE_RESPONSE_FILE, this env var must be declared in the manifest `env` block for `buildStepEnv` to forward it to the claude shim. Verify by reading step-runner.ts `buildStepEnv` — it includes env vars from `envVars` parameter.

    **Prompt files:** All prompt .md files can contain minimal placeholder text like "Perform the step." — the mock shim ignores prompt content entirely.
  </action>
  <verify>
    <automated>ls tests/e2e/fixtures/agents/multi-step-failure-agent/manifest.yaml tests/e2e/fixtures/agents/retry-agent/manifest.yaml tests/e2e/fixtures/agents/timeout-agent/manifest.yaml tests/e2e/fixtures/mock-claude/responses/failure.json tests/e2e/fixtures/mock-claude/responses/retry-fail.json && echo "All fixture files exist"</automated>
  </verify>
  <done>
    Three error-scenario fixture agents exist with valid manifests, prompt files, and response JSON files. Each agent is designed to trigger a specific engine error path (semantic failure, retry exhaustion, timeout).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write error scenario E2E tests</name>
  <files>tests/e2e/error-scenarios.test.ts</files>
  <behavior>
    - Test: multi-step-failure-agent produces inbox report with failed status and error message
    - Test: timeout-agent produces inbox report with timed-out status
    - Test: retry-agent exhausts retries and produces a report (verify retry count or final state)
    - Test: each error scenario generates appropriate ntfy failure notifications
  </behavior>
  <action>
    Create `tests/e2e/error-scenarios.test.ts`.

    **Setup (beforeEach):**
    - Create temp directory
    - Start ntfy mock server
    - Call `writeE2EConfig(tmpDir, { ntfyPort, agentNames: ["multi-step-failure-agent", "retry-agent", "timeout-agent", "happy-path-agent"] })`
    - Build env with mock-claude PATH prepended
    - Start daemon

    **Teardown (afterEach):**
    - killDaemon (safety net)
    - Close ntfy mock server
    - Remove temp directory

    **Test: "semantic failure — agent returns status:FAILED"**
    1. Submit multi-step-failure-agent
    2. Wait for inbox report matching `/multi-step-failure/`
    3. Assert report contains `status: failed` (or equivalent — check reporter.ts for exact field names)
    4. Assert report contains the error message from failure.json ("mock failure: step1 encountered an error")
    5. Assert step2 did NOT execute (report should show only 1 step attempted, or step2 not mentioned)
    6. Verify ntfy mock received a failure notification

    **Test: "timeout — agent exceeds step timeout"**
    1. Submit timeout-agent
    2. Wait for inbox report matching `/timeout-agent/` — use a generous poll timeout (30-45s) since the daemon needs to detect the 2s timeout and write the report
    3. Assert report contains timeout-related status (check reporter.ts — may be "failed" with timeout info, or a specific "timed-out" status)
    4. Verify ntfy mock received a failure/timeout notification

    **Test: "retry exhaustion — agent retries maxAttempts then continues"**
    1. Submit retry-agent
    2. Wait for inbox report matching `/retry-agent/`
    3. Check the report — after retry exhaustion, engine continues to next step or completes. The exact behavior depends on engine.ts: after maxAttempts retries, the engine logs "retry exhausted" and continues the while loop (i++ proceeds to next step). The report should show the agent completed but with retry information.
    4. Read engine.ts carefully during implementation to understand what the report looks like after retry exhaustion — it may complete with partial results or a specific status.

    **Polling helper:** Reuse or create a `waitForInboxReport(cwd, pattern, timeoutMs)` helper that polls the inbox directory. If this was already created in plan 02, import it. If not, define it locally or add to helpers.

    **Key detail:** Each test submits ONE agent and waits for its specific report. Tests should be independent — each can run in isolation. The daemon may process tasks in any order if multiple are submitted, so submit one at a time per test.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.e2e.config.ts tests/e2e/error-scenarios.test.ts</automated>
  </verify>
  <done>
    All error scenario tests pass: semantic failure produces failed report with error message, timeout produces timed-out report, retry exhaustion is handled correctly. ntfy notifications sent for each error type. No real claude binary called.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --config vitest.e2e.config.ts tests/e2e/error-scenarios.test.ts` passes
- Each error scenario produces a distinct inbox report
- Failure report shows error message from mock response
- Timeout report shows timeout-related status
- Retry report shows exhaustion behavior
- ntfy mock captures failure notifications
</verification>

<success_criteria>
- 3 fixture agents created: multi-step-failure-agent, retry-agent, timeout-agent
- 2 response files created: failure.json, retry-fail.json
- error-scenarios.test.ts has 3+ tests covering failure, timeout, retry exhaustion
- All tests pass with correct assertions on inbox report content
- Full E2E suite passes: `npm run test:e2e` (all 4 test files)
</success_criteria>

<output>
After completion, create `.planning/phases/17-e2e-testing-framework/17-03-SUMMARY.md`
</output>
