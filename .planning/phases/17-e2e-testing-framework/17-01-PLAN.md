---
phase: 17-e2e-testing-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.e2e.config.ts
  - package.json
  - tests/e2e/helpers/daemon.ts
  - tests/e2e/helpers/ntfy-server.ts
  - tests/e2e/helpers/config.ts
  - tests/e2e/helpers/cli.ts
  - tests/e2e/fixtures/mock-claude/claude
  - tests/e2e/fixtures/mock-claude/responses/success.json
  - tests/e2e/fixtures/agents/happy-path-agent/manifest.yaml
  - tests/e2e/fixtures/agents/happy-path-agent/prompts/run.md
  - tests/e2e/lifecycle.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-05

must_haves:
  truths:
    - "E2E test suite can be invoked via npm run test:e2e"
    - "A daemon process starts from a test, writes heartbeat, and can be stopped"
    - "Crash recovery works: SIGKILL daemon, stale PID detected, restart succeeds"
    - "Mock claude shim intercepts all claude invocations — no real claude binary called"
    - "ntfy mock server captures HTTP POST requests on localhost"
  artifacts:
    - path: "vitest.e2e.config.ts"
      provides: "Separate vitest config for E2E suite with extended timeouts and serial execution"
    - path: "tests/e2e/helpers/daemon.ts"
      provides: "startDaemon, waitForDaemonReady, stopDaemon, killDaemon helper functions"
      exports: ["startDaemon", "waitForDaemonReady", "stopDaemon", "killDaemon"]
    - path: "tests/e2e/helpers/ntfy-server.ts"
      provides: "createNtfyMockServer — localhost HTTP mock that records requests"
      exports: ["createNtfyMockServer"]
    - path: "tests/e2e/helpers/config.ts"
      provides: "writeE2EConfig — generates nightshift.yaml for test environment"
      exports: ["writeE2EConfig"]
    - path: "tests/e2e/helpers/cli.ts"
      provides: "run() helper — spawns CLI commands against test workspace"
      exports: ["run"]
    - path: "tests/e2e/fixtures/mock-claude/claude"
      provides: "Shell script shim that reads MOCK_CLAUDE_RESPONSE_FILE and emits ClaudeJsonOutput"
    - path: "tests/e2e/fixtures/agents/happy-path-agent/manifest.yaml"
      provides: "Single-step test agent with MOCK_CLAUDE_RESPONSE_FILE env declaration"
    - path: "tests/e2e/lifecycle.test.ts"
      provides: "Daemon lifecycle E2E tests (start, heartbeat, stop, crash recovery)"
  key_links:
    - from: "tests/e2e/helpers/daemon.ts"
      to: "bin/nightshift.ts"
      via: "spawn npx tsx bin/nightshift.ts start/stop"
      pattern: "spawnWithTimeout.*nightshift"
    - from: "tests/e2e/fixtures/mock-claude/claude"
      to: "src/agent/step-runner.ts"
      via: "PATH shim intercepts spawn('claude', ...)"
      pattern: "MOCK_CLAUDE_RESPONSE_FILE"
    - from: "tests/e2e/helpers/ntfy-server.ts"
      to: "nightshift.yaml ntfy.baseUrl"
      via: "localhost URL override in test config"
      pattern: "http://127.0.0.1"
---

<objective>
Create the full E2E test infrastructure and validate it with daemon lifecycle tests.

Purpose: This plan builds every shared helper, fixture, and config file that all E2E tests need. The lifecycle test proves the infrastructure works end-to-end: start a real daemon, verify heartbeat, stop it gracefully, and recover from a crash.

Output: Working `npm run test:e2e` command, daemon helpers, mock claude shim, ntfy mock server, happy-path fixture agent, and passing lifecycle tests.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-e2e-testing-framework/17-CONTEXT.md
@.planning/phases/17-e2e-testing-framework/17-RESEARCH.md

@vitest.config.ts
@package.json
@src/agent/step-runner.ts
@src/daemon/health.ts
@src/daemon/orchestrator.ts
@src/cli/commands/start.ts
@src/cli/commands/stop.ts
@src/utils/process.ts
@tests/integration/submit.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/agent/step-runner.ts:
```typescript
// ClaudeJsonOutput shape the mock shim must emit:
// { session_id: string, result: string, is_error: boolean, duration_ms: number, total_cost_usd: number, num_turns: number }

export function buildStepEnv(
  stepName: string,
  envVars?: Array<{ name: string; value: string }>,
): NodeJS.ProcessEnv;
// Whitelist: HOME, PATH, USER, LANG, SHELL, TERM + declared step envVars
```

From src/daemon/health.ts:
```typescript
export async function readDaemonState(base?: string): Promise<DaemonState | null>;
export function isDaemonRunning(state: DaemonState): boolean;
// DaemonState has: pid, status, lastHeartbeat, startedAt, ...
```

From src/utils/process.ts:
```typescript
export function spawnWithTimeout(cmd: string, args: string[], opts: {
  timeoutMs: number; cwd?: string; env?: NodeJS.ProcessEnv;
}): { result: Promise<SpawnResult>; kill: () => void };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E vitest config, helpers, fixtures, and mock claude shim</name>
  <files>
    vitest.e2e.config.ts,
    package.json,
    tests/e2e/helpers/daemon.ts,
    tests/e2e/helpers/ntfy-server.ts,
    tests/e2e/helpers/config.ts,
    tests/e2e/helpers/cli.ts,
    tests/e2e/fixtures/mock-claude/claude,
    tests/e2e/fixtures/mock-claude/responses/success.json,
    tests/e2e/fixtures/agents/happy-path-agent/manifest.yaml,
    tests/e2e/fixtures/agents/happy-path-agent/prompts/run.md
  </files>
  <action>
    **vitest.e2e.config.ts** — Create a separate vitest config at project root:
    - `include: ["tests/e2e/**/*.test.ts"]`
    - `testTimeout: 120_000` (2 minutes per test — daemon lifecycle is slow)
    - `hookTimeout: 30_000` (30s for beforeEach/afterEach)
    - `pool: "forks"` with `poolOptions.forks.singleFork: true` (serial execution — no daemon port conflicts)
    - `reporters: ["verbose"]`

    **package.json** — Add `"test:e2e": "vitest run --config vitest.e2e.config.ts"` script. Do NOT modify the existing `"test"` script — it must continue to run only unit + integration tests. Update the existing `"test"` script include pattern to exclude `tests/e2e/` if it currently uses `tests/**/*.test.ts`. Check the vitest.config.ts include pattern — if it includes `tests/**/*.test.ts`, narrow it to `["tests/unit/**/*.test.ts", "tests/integration/**/*.test.ts"]` so that `npm test` does not pick up E2E tests.

    **tests/e2e/helpers/daemon.ts** — Daemon lifecycle helpers:
    - `startDaemon(cwd: string, env: NodeJS.ProcessEnv): Promise<DaemonHandle>` — spawns `npx tsx bin/nightshift.ts start` with the given cwd and env (which includes the mock-claude PATH prepended). Waits for the start command to exit with code 0, then calls `waitForDaemonReady`.
    - `waitForDaemonReady(cwd: string, maxWaitMs = 30_000): Promise<void>` — polls `{cwd}/.nightshift/daemon.json` every 200ms until `status === "running"` and `lastHeartbeat` is within 5 seconds.
    - `stopDaemon(cwd: string): Promise<void>` — runs `npx tsx bin/nightshift.ts stop` with cwd.
    - `killDaemon(cwd: string): Promise<void>` — reads daemon.json to get PID, sends SIGKILL. Idempotent (catches if process already dead).
    - Export `DaemonHandle` interface: `{ pid: number | undefined; cwd: string }`.
    - Use `path.resolve("bin/nightshift.ts")` for the CLI binary path (absolute, from project root).
    - Use `spawnWithTimeout` from `src/utils/process.ts` for the stop command.

    **tests/e2e/helpers/ntfy-server.ts** — Ntfy mock server:
    - `createNtfyMockServer(): Promise<NtfyMockServer>` — creates `http.createServer` on port 0 (OS-assigned), 127.0.0.1.
    - Records all incoming requests as `{ method, path, body }`.
    - Returns `{ port, getRequests(), close() }`.
    - Response: always 200 with `{ id: "mock-id" }`.

    **tests/e2e/helpers/config.ts** — Config writer:
    - `writeE2EConfig(tmpDir: string, options: { ntfyPort?: number; agentNames?: string[]; pollIntervalMs?: number; heartbeatIntervalMs?: number }): Promise<void>`
    - Writes `nightshift.yaml` to tmpDir with:
      - `workspace: ./workspace`, `inbox: ./inbox`
      - `agents_dir: ./agents` (the test copies fixture agents here)
      - `daemon.poll_interval_ms: 500` (fast task pickup)
      - `daemon.heartbeat_interval_ms: 1000` (fast readiness detection)
      - ntfy block pointing to `http://127.0.0.1:{ntfyPort}` if provided
      - agents list from agentNames param (defaults to `["happy-path-agent"]`)
    - Also creates the `.nightshift/` directory structure (queue/, inbox/, logs/).
    - Also copies fixture agent directories from `tests/e2e/fixtures/agents/` into `{tmpDir}/agents/`.

    **tests/e2e/helpers/cli.ts** — CLI run helper:
    - `run(args: string[], options: { cwd: string; env?: NodeJS.ProcessEnv }): Promise<{ stdout: string; stderr: string; exitCode: number | null }>` — uses `spawnWithTimeout` from `src/utils/process.ts` to run `npx tsx bin/nightshift.ts ...args`. Timeout 15s.
    - Reuse the same pattern as existing integration tests.

    **tests/e2e/fixtures/mock-claude/claude** — Shell script shim:
    - `#!/usr/bin/env bash`
    - If `MOCK_CLAUDE_SLEEP_MS` is set, sleep that many milliseconds (convert to seconds via `bc` or bash arithmetic).
    - If `MOCK_CLAUDE_RESPONSE_FILE` is set and the file exists, `cat` it and exit 0.
    - Otherwise, emit a default success JSON: `{"session_id":"mock","result":"` + `` ```json\n{\"result\":\"default mock output\"}\n``` `` + `","is_error":false,"duration_ms":100,"total_cost_usd":0,"num_turns":1}` and exit 0.
    - MUST be committed with execute permission. Run `chmod +x` after creating. Also add a step in the config helper or test setup to `chmod +x` the shim (git may not preserve permissions).

    **tests/e2e/fixtures/mock-claude/responses/success.json** — Canned success response:
    ```json
    {"session_id":"mock-session","result":"```json\n{\"result\":\"mock success output\"}\n```","is_error":false,"duration_ms":150,"total_cost_usd":0.001,"num_turns":1}
    ```

    **tests/e2e/fixtures/agents/happy-path-agent/manifest.yaml** — Single-step test agent:
    ```yaml
    name: happy-path-agent
    description: Single-step agent for E2E happy-path testing
    steps:
      - name: run
        prompt: prompts/run.md
        timeout: "30s"
        outputSchema:
          type: object
          properties:
            result:
              type: string
          required: [result]
        env:
          - name: MOCK_CLAUDE_RESPONSE_FILE
            value: "{{response_file}}"
    ```
    The `{{response_file}}` template variable will be set to the absolute path of `success.json` at test time. Check how manifest template variables are resolved — if they require task-level variables, set those when submitting the task. Alternatively, if template variables are not supported in env values, hard-code the absolute path at fixture copy time (the config helper can rewrite the manifest when copying agents to tmpDir).

    **tests/e2e/fixtures/agents/happy-path-agent/prompts/run.md** — Minimal prompt:
    ```
    Analyze the codebase and provide a summary.
    ```
    (Content doesn't matter — the mock shim ignores the prompt and returns the canned response.)
  </action>
  <verify>
    <automated>npx tsc --noEmit && ls tests/e2e/helpers/daemon.ts tests/e2e/helpers/ntfy-server.ts tests/e2e/helpers/config.ts tests/e2e/helpers/cli.ts tests/e2e/fixtures/mock-claude/claude tests/e2e/fixtures/mock-claude/responses/success.json tests/e2e/fixtures/agents/happy-path-agent/manifest.yaml</automated>
  </verify>
  <done>
    All E2E infrastructure files exist and type-check. vitest.e2e.config.ts created. package.json has test:e2e script. npm test does NOT include E2E tests. Mock claude shim is executable. Fixture agents have valid manifest + prompt files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write daemon lifecycle E2E tests</name>
  <files>tests/e2e/lifecycle.test.ts</files>
  <behavior>
    - Test: daemon starts and writes heartbeat to daemon.json within 30s
    - Test: daemon status shows "running" after start
    - Test: daemon stops gracefully via CLI stop command (exit code 0, daemon.json status changes)
    - Test: crash recovery — SIGKILL daemon, verify stale PID detected by health module, subsequent start recovers
    - Test: ntfy mock server receives daemon-related notifications (if applicable)
  </behavior>
  <action>
    Create `tests/e2e/lifecycle.test.ts` with a `describe("daemon lifecycle")` block.

    **Setup (beforeEach):**
    - Create temp directory via `fs.mkdtemp`
    - Call `writeE2EConfig(tmpDir, { ntfyPort })` to set up config + copy agents
    - Ensure mock claude shim is `chmod +x`
    - Build the env object with mock-claude dir prepended to PATH

    **Teardown (afterEach):**
    - Call `killDaemon(tmpDir)` unconditionally (safety net — idempotent)
    - Clean up temp directory via `fs.rm(tmpDir, { recursive: true, force: true })`
    - Close ntfy mock server if started

    **Test: "starts daemon and writes heartbeat"**
    - Call `startDaemon(tmpDir, env)` (includes waitForReady)
    - Read `.nightshift/daemon.json` — assert `status === "running"` and `lastHeartbeat` is recent
    - Read `.nightshift/daemon.pid` — assert PID file exists and contains a number
    - Stop daemon

    **Test: "stops daemon gracefully via CLI"**
    - Start daemon
    - Call `stopDaemon(tmpDir)`
    - Read daemon.json — assert status is no longer "running" (or file is cleaned up)
    - Verify process with that PID is no longer alive (use `process.kill(pid, 0)` in try/catch)

    **Test: "recovers from crash (SIGKILL)"**
    - Start daemon, get PID
    - Call `killDaemon(tmpDir)` (sends SIGKILL)
    - Wait a moment (500ms) for the process to die
    - Call `startDaemon(tmpDir, env)` again — should succeed (stale PID detection + cleanup)
    - Verify new daemon is running with a different PID
    - Stop daemon

    **Test: "status command shows running daemon"**
    - Start daemon
    - Run `run(["status"], { cwd: tmpDir, env })` via CLI helper
    - Assert stdout contains "running" or the daemon PID
    - Stop daemon

    Use the `run()` helper from `tests/e2e/helpers/cli.ts` for CLI commands. Use the daemon helpers for lifecycle management.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.e2e.config.ts tests/e2e/lifecycle.test.ts</automated>
  </verify>
  <done>
    All daemon lifecycle tests pass: start writes heartbeat, stop terminates gracefully, crash recovery works, status CLI shows running state. No real claude binary is called (mock shim on PATH).
  </done>
</task>

</tasks>

<verification>
- `npm run test:e2e` runs and lifecycle tests pass
- `npm test` still runs unit + integration tests only (does NOT include E2E)
- No real `claude` binary is invoked during test run
- Daemon processes are cleaned up after each test (no orphaned processes)
</verification>

<success_criteria>
- vitest.e2e.config.ts exists with serial execution and extended timeouts
- package.json has test:e2e script
- All 4 helper modules (daemon, ntfy-server, config, cli) exist and export their functions
- Mock claude shim is executable and returns valid ClaudeJsonOutput JSON
- Happy-path-agent fixture has manifest.yaml + prompt file
- lifecycle.test.ts has 4+ tests covering start, stop, crash recovery, status
- All lifecycle tests pass via `npx vitest run --config vitest.e2e.config.ts tests/e2e/lifecycle.test.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/17-e2e-testing-framework/17-01-SUMMARY.md`
</output>
