---
phase: 12-fix-scheduler-dispatch-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/daemon/orchestrator.ts
  - tests/unit/orchestrator.test.ts
autonomous: true
requirements:
  - WIRE-01

must_haves:
  truths:
    - "Scheduled tasks from evaluateSchedules() are dispatched to AgentPool"
    - "Scheduled task dispatch respects pool capacity (canAccept check)"
    - "notifyTaskStart() is called for each dispatched scheduled task"
  artifacts:
    - path: "src/daemon/orchestrator.ts"
      provides: "Wired scheduler dispatch in tick()"
      contains: "scheduledTasks"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Unit test verifying scheduled task dispatch flow"
      contains: "scheduled tasks"
  key_links:
    - from: "src/daemon/orchestrator.ts"
      to: "scheduler.evaluateSchedules()"
      via: "captured return value dispatched to pool"
      pattern: "const scheduledTasks.*evaluateSchedules"
    - from: "src/daemon/orchestrator.ts"
      to: "pool.dispatch()"
      via: "loop over scheduledTasks with canAccept guard"
      pattern: "this\\.pool\\.dispatch\\(task\\)"
---

<objective>
Fix the scheduler dispatch wiring bug: capture the `evaluateSchedules()` return value in `orchestrator.ts` tick() and dispatch scheduled tasks to `AgentPool`.

Purpose: Scheduled agents configured in `nightshift.yaml` with cron expressions are correctly evaluated but never dispatched because the return value is discarded on line 237. This closes the last WIRE-01 gap.
Output: Fixed orchestrator.ts + unit test proving the wiring works.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-scheduler-dispatch-wiring/12-CONTEXT.md
@.planning/phases/12-fix-scheduler-dispatch-wiring/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/daemon/scheduler.ts:
```typescript
async evaluateSchedules(): Promise<NightShiftTask[]>
```

From src/daemon/agent-pool.ts:
```typescript
canAccept(): boolean
dispatch(task: NightShiftTask): void
```

From src/daemon/orchestrator.ts (private method):
```typescript
private notifyTaskStart(task: NightShiftTask): void
```

From src/core/types.ts:
```typescript
interface NightShiftTask {
  id: string;
  name: string;
  origin: string;       // "recurring" for scheduled tasks
  prompt: string;
  status: string;
  timeout: string;
  createdAt: string;
  agentName?: string;
  variables?: Record<string, string>;
  notify?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit test for scheduled task dispatch wiring</name>
  <files>tests/unit/orchestrator.test.ts</files>
  <behavior>
    - Test: evaluateSchedules() returns tasks -> each task is dispatched via pool.dispatch() and notifyTaskStart()
    - Test: when pool.canAccept() returns false, remaining scheduled tasks are skipped (not dispatched)
    - Test: when evaluateSchedules() returns empty array, no dispatch calls are made
  </behavior>
  <action>
Add a new `describe("scheduled task dispatch")` block to `tests/unit/orchestrator.test.ts`. Use the project's established pattern of accessing private methods via `(orchestrator as any)` (see existing tests at lines 325-387).

Test approach: Mock `this.scheduler.evaluateSchedules` to return a known `NightShiftTask[]`, mock `this.pool.canAccept` and `this.pool.dispatch`, mock `this.notifyTaskStart`. Then call `(orchestrator as any).tick()` and assert:

1. **Happy path:** evaluateSchedules returns 2 tasks, canAccept returns true -> dispatch called twice, notifyTaskStart called twice
2. **Pool full:** evaluateSchedules returns 2 tasks, canAccept returns true then false -> dispatch called once, second task skipped
3. **No scheduled tasks:** evaluateSchedules returns [] -> dispatch never called

Important: tick() also calls `loadConfig()`, `this.pool.collectCompleted()`, and `this.getReadyTasks()`. Mock those to return neutral values (empty arrays, existing config) so the test isolates the scheduler dispatch path. Use `vi.spyOn` on the orchestrator instance's private properties.

These tests MUST fail initially (tick() currently discards evaluateSchedules return value).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/orchestrator.test.ts -t "scheduled task dispatch" 2>&1 | tail -20</automated>
  </verify>
  <done>Three test cases exist for scheduled task dispatch wiring. Tests fail because tick() discards evaluateSchedules() return value (RED phase).</done>
</task>

<task type="auto">
  <name>Task 2: Wire evaluateSchedules() return value into pool.dispatch()</name>
  <files>src/daemon/orchestrator.ts</files>
  <action>
In `src/daemon/orchestrator.ts` tick() method, replace line 237:

```typescript
await this.scheduler.evaluateSchedules();
```

With the dispatch loop:

```typescript
const scheduledTasks = await this.scheduler.evaluateSchedules();
for (const task of scheduledTasks) {
  if (!this.pool.canAccept()) break;
  this.pool.dispatch(task);
  this.notifyTaskStart(task);
}
```

This goes BEFORE the queue-based dispatch block (lines 246-257) so scheduled tasks get priority (they have a cron window). Keep the existing comment updated: change "Evaluate cron schedules" to "Evaluate cron schedules and dispatch due tasks".

Do NOT:
- Add claimTask() for scheduled tasks (they come from evaluateSchedules, not the file queue)
- Add try/catch beyond what exists (evaluateSchedules handles its own errors)
- Change any other part of tick()
  </action>
  <verify>
    <automated>npx vitest run tests/unit/orchestrator.test.ts tests/unit/scheduler.test.ts tests/unit/agent-pool.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All three scheduled dispatch tests pass (GREEN). Existing orchestrator, scheduler, and agent-pool tests still pass. The evaluateSchedules() return value is captured and dispatched with canAccept guard and notifyTaskStart calls.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/orchestrator.test.ts` -- all tests pass including new scheduled dispatch tests
2. `npx vitest run` -- full suite green, no regressions
3. `grep -n "scheduledTasks" src/daemon/orchestrator.ts` -- confirms the return value is captured
4. `grep -n "dispatch" src/daemon/orchestrator.ts` -- shows dispatch called for both scheduled and queue tasks
</verification>

<success_criteria>
- orchestrator.ts tick() captures evaluateSchedules() return value and dispatches each task to pool
- Unit test verifies: scheduled tasks flow from evaluateSchedules() through canAccept() to dispatch() + notifyTaskStart()
- Unit test verifies: pool capacity is respected (break when canAccept() returns false)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-scheduler-dispatch-wiring/12-01-SUMMARY.md`
</output>
