---
phase: 16-codebase-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/agent/agent-types.ts
  - src/agent/scaffold.ts
  - src/agent/engine-types.ts
  - src/agent/step-runner.ts
  - src/core/types.ts
  - src/core/config.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "agent-types.ts no longer exists — validateAgentName lives in scaffold.ts"
    - "No stale comments referencing deleted files (code-agent-runner.ts, agent-runner.ts, Phase 10 migration)"
    - "NightShiftTask has no category field"
    - "Stale Zod schema fields removed from config.ts if confirmed dead"
    - "All tests pass after cleanup"
  artifacts:
    - path: "src/agent/scaffold.ts"
      provides: "validateAgentName function (moved from agent-types.ts)"
      contains: "validateAgentName"
  key_links:
    - from: "src/agent/scaffold.ts"
      to: "validateAgentName"
      via: "function defined locally (no longer imported)"
      pattern: "function validateAgentName"
---

<objective>
Remove dead exports, merge tiny files, clean stale comments, and remove vestigial type/schema fields.

Purpose: Final cleanup pass — eliminate orphaned type exports in agent-types.ts (moving the one live function into scaffold.ts), scrub stale comments referencing deleted v1.0/v2.0 files, and remove dead fields from NightShiftTask and the Zod config schema.

Output: 1 file deleted (agent-types.ts), 5 files modified.
</objective>

<execution_context>
@/Users/julienderay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienderay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-codebase-cleanup/16-RESEARCH.md
@.planning/phases/16-codebase-cleanup/16-02-SUMMARY.md

<interfaces>
<!-- agent-types.ts — only validateAgentName is live, rest is dead -->
From src/agent/agent-types.ts:
```typescript
export function validateAgentName(name: string): { valid: boolean; error?: string };
// DEAD: AgentConfig, PipelineContext, AgentRunOutcome, AgentRunResult, HandoffPayload
```

From src/agent/scaffold.ts (imports validateAgentName):
```typescript
import { validateAgentName } from "./agent-types.js";
```

From src/core/types.ts (dead fields):
```typescript
export interface NightShiftTask {
  // ...
  category?: string;  // DEAD — never set or read at runtime
  // ...
}
```

From src/agent/engine-types.ts (stale comment at lines ~53-54):
```typescript
// references "PipelineContext in code-agent-runner.ts" — that file was deleted in v2.0
```

From src/agent/step-runner.ts (stale comment at line ~63):
```typescript
// references "the existing AgentRunner.buildArgs pattern in agent-runner.ts" — deleted
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move validateAgentName into scaffold.ts, delete agent-types.ts</name>
  <files>src/agent/agent-types.ts, src/agent/scaffold.ts</files>
  <action>
1. Read src/agent/scaffold.ts to understand its current structure and where validateAgentName is called.

2. Copy the validateAgentName function (lines 1-16 of agent-types.ts) into scaffold.ts. Place it near the top of the file, after imports but before the main scaffold logic. Keep it as a non-exported function if only used within scaffold.ts, or keep it exported if other files import it.
   - Check: `grep -r "validateAgentName" src/ tests/ --include="*.ts"` to find all consumers.
   - If scaffold.ts is the ONLY consumer, make it a plain `function` (not exported).
   - If tests also import it from agent-types.ts, update those imports to point to scaffold.ts instead and keep it exported.

3. Remove the import of validateAgentName from scaffold.ts (it is now local).

4. Delete src/agent/agent-types.ts entirely.

5. Verify no file imports from agent-types.ts: `grep -r "agent-types" src/ tests/ --include="*.ts"` should return zero hits.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>agent-types.ts is deleted. validateAgentName lives in scaffold.ts. No file imports from agent-types. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale comments, dead type fields, and vestigial Zod fields</name>
  <files>src/agent/engine-types.ts, src/agent/step-runner.ts, src/core/types.ts, src/core/config.ts</files>
  <action>
1. src/agent/engine-types.ts: Find and remove the comment at lines ~53-54 that references "PipelineContext in code-agent-runner.ts". Remove only the comment — do NOT change the type definition it annotates.

2. src/agent/step-runner.ts: Find and remove the comment at line ~63 referencing "the existing AgentRunner.buildArgs pattern in agent-runner.ts". Remove only the comment.

3. src/core/types.ts:
   - Remove the `category?: string` field from NightShiftTask interface (line 21). It is never set or read at runtime — a v1.0 vestige.
   - Clean the stale comment on agentName: change `agentName?: string;  // kebab-case agent name; required after Phase 10 migration` to just `agentName?: string;` (remove the "Phase 10 migration" part, or replace with a simpler comment like `// kebab-case agent name`).

4. src/core/config.ts — Zod schema audit:
   - Verify whether `one_off_defaults.max_budget_usd` (mapped to `oneOffDefaults.maxBudgetUsd`) is used by any runtime code. Run: `grep -r "maxBudgetUsd" src/ --include="*.ts"` (excluding config.ts itself and types.ts).
   - If NO runtime code reads or writes maxBudgetUsd (beyond schema definition and type declaration), remove it from:
     a. The Zod ConfigSchema in config.ts (the `max_budget_usd` field under `one_off_defaults`)
     b. The OneOffDefaults interface in types.ts
     c. The NightShiftTask `maxBudgetUsd?` field in types.ts (if also confirmed dead)
   - If it IS wired somewhere, leave it. Document what you found.
   - IMPORTANT: Also check if any nightshift.yaml files in agent directories reference max_budget_usd. If real config files use it, leave the Zod field even if no runtime code reads it yet.

5. Run the full test suite.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All stale comments removed. category field removed from NightShiftTask. Vestigial Zod fields removed if confirmed dead. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- `ls src/agent/agent-types.ts` returns "No such file or directory"
- `grep -r "agent-types" src/ tests/ --include="*.ts"` returns zero hits
- `grep "category" src/core/types.ts` returns zero hits
- `grep -r "code-agent-runner" src/ --include="*.ts"` returns zero hits
- `grep -r "agent-runner.ts" src/ --include="*.ts"` returns zero hits
- `npx vitest run` passes with zero failures
</verification>

<success_criteria>
- agent-types.ts deleted, validateAgentName relocated to scaffold.ts
- Zero stale comments referencing deleted files (code-agent-runner.ts, agent-runner.ts)
- NightShiftTask.category field removed
- Vestigial Zod fields removed (if confirmed dead)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-codebase-cleanup/16-03-SUMMARY.md`
</output>
